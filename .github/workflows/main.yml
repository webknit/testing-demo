name: CI
jobs:
  build:
    runs-on: ubuntu-latest
      - uses: actions/checkout@v3
      
      - name: build
        run: npm run build
      
      - name: Percy Test
        run: npx @percy/cli snapshot public/ # or any directory such as public/