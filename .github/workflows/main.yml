name: CI
jobs:
  build:
    runs-on: ubuntu-latest
      - uses: actions/checkout@v3
      
      - name: build
        run: npm run build
      
      - name: Percy Test
        run:
        - npm run start
        - npx percy snapshot snapshots.yml